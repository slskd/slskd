name: Mirror Repository

on:
  push:
    branches:
      - '**'  # Trigger on push to any branch
  delete:
    branches:
      - '**'  # Trigger when branches are deleted
  workflow_dispatch:  # Allow manual triggering from GitHub UI
    inputs:
      target:
        description: 'Target to mirror to'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - GitLab
          - Codeberg
      delay:
        description: 'Delay in seconds before mirroring'
        required: false
        default: '0'

concurrency:
  group: mirror-sync
  cancel-in-progress: false

jobs:
  mirror:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target:
          - name: GitLab
            host: gitlab.com
            repo: git@gitlab.com:slskd/slskd.git
          - name: Codeberg
            host: codeberg.org
            repo: git@codeberg.org:slskd/slskd.git
      fail-fast: false  # Continue with other mirrors even if one fails

    steps:
    - name: Wait for things to settle
      run: |
        echo "⏳ Waiting 15 seconds..."
        sleep 15

    - name: Checkout repository
      if: github.event_name != 'delete'  # Skip checkout for delete events
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch full history for proper mirroring
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Configure SSH
      env:
        SSH_KEY: ${{ secrets.GIT_MIRROR_SSH_KEY }}
      run: |
        if [ -z "$SSH_KEY" ]; then
          echo "⚠️ SSH key not configured. Skipping mirror sync."
          echo "Please set GIT_MIRROR_SSH_KEY secret to enable mirroring."
          exit 1
        fi

        # Set up SSH key
        mkdir -p ~/.ssh
        echo "$SSH_KEY" > ~/.ssh/id_ed25519
        chmod 600 ~/.ssh/id_ed25519

        # Start SSH agent and add key
        eval "$(ssh-agent -s)"
        ssh-add ~/.ssh/id_ed25519

        # Add known host to avoid SSH prompts
        ssh-keyscan ${{ matrix.target.host }} >> ~/.ssh/known_hosts

        # Configure Git
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"

    - name: Mirror to ${{ matrix.target.name }}
      run: |
        echo "🔄 Mirroring repository to ${{ matrix.target.name }}..."
        
        # Clone the GitHub repository as a mirror (bare repo with all refs)
        git clone --mirror https://github.com/${{ github.repository }}.git mirror-repo
        cd mirror-repo
        
        # Set the push URL to the target repository
        git remote set-url --push origin ${{ matrix.target.repo }}
        
        # Push everything to the mirror (handles all branches, tags, and deletions)
        git push --mirror
        
        echo "✅ Successfully mirrored to ${{ matrix.target.name }}"